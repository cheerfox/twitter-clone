<% if logged_in? %>
  <h1><%= "Welcome #{current_user.username}" %></h1>
<% end %>
<%= render 'shared/header', header: 'All users' %>
<ul>
  <% @users.each do |user| %>
    <li><%= link_to user.username, user_path(user.username) %></li>
  <% end %>
</ul>
